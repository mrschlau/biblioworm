<h1>Listing books</h1>

<table>
  <tr>
    <th>Contributing worm</th>
    <th>Genre</th>
    <th>Title</th>
    <th>Author</th>
    <th>Synopsis</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= book.worm_id %></td>
    <td><%= book.genre %></td>
    <td><%= book.title %></td>
    <td><%= book.author %></td>
    <td><%= book.synopsis %></td>
    <td><%= link_to 'Show', book %></td>
    <td><%= link_to 'Edit', edit_book_path(book) %></td>
    <td><%= link_to 'Destroy', book, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
